# wwbs 1.4.5 正式版

本版本继续提升一键日常与4C刷取的稳定性，并修复 Windows 任务栏图标。

- 点击任何真实运行按钮时先检查管理员权限；若 wwbs 未使用管理员权限，立即弹出 Windows UAC，允许后以管理员身份重启并关闭旧实例，不再等待任务报错。
- 预演模式不触发管理员权限请求；即使日常和周常已经完成，也会在真实任务开始前完成权限检查。
- 若用户拒绝 UAC 或提权启动失败，保留原有错误提示与后台诊断，不会反复拉起。
- 使用 Windows 原生窗口消息为真正的顶层窗口设置大小图标，修复部分环境中任务栏仍显示 Tk 羽毛图标的问题。
- 4C刷取的5次和10次专用按钮始终使用随程序提供的 `4c` 模板，不需要在模板管理中预先切换，也不会修改用户当前模板选择。
- 日常战斗结束后的奖励光球最低置信度由0.25提高到0.65，降低广告牌、反光等画面元素造成的误判。
- 周本技能槽只以中央“＋ / 选择祝福”作为未选择标志；只要不是加号，选择任意祝福都能直接开始，不再要求使用第一个祝福。

下载 `wwbs-exe.zip`，完整解压后运行 `wwbs.exe`。任务执行遇到权限问题时，请在 Windows 管理员权限提示中选择“是”。
